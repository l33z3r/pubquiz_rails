-if thing.errors.any?
  .panel.panel-danger
    .panel-heading
      =pluralize(thing.errors.count, 'error')
      =message || 'prevented you from saving:'

    .panel-body
      %ul
        -thing.errors.full_messages.each do |msg|
          %li
            =msg
